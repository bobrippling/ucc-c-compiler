Everything must have a type (for casts later on)
i.e.
	t->type, instead of just for identifiers, etc

fix:
	int main(){1;}
	pop 1 from the stack

*(int *)5 = 2;
general lhs assignment:
	(b ? x : y) = 5;
