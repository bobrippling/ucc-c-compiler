Ordered By Priority
-------------------

for(;;){
}

duplicate symbol check: "int x; x(){}"

typedefs

struct parsing

32-bit code gen

general lhs assignment:
	(b ? x : y) = 5;
	^-- note: gnu extension
	(x ? a : b)(2); // similarly
	(x = y) = 2; // assign to x

assembly optimisation - remove asm_temp()

({ expr; })
parsing

for(...){
}else{
	printf("for not entered");
}
