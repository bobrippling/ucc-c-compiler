Ordered By Priority
-------------------

char *s = "hi";
	the symbol s is initialised as the start of "hi", rather than a
	pointer to the data "hi"

s/ICE/UCC_ASSERT/

errno - particularly BSD-systems

typedefs

struct parsing

32-bit code gen

general lhs assignment:
	(b ? x : y) = 5;
	^-- note: gnu extension
	(x ? a : b)(2); // similarly
	(x = y) = 2; // assign to x

assembly optimisation - remove asm_temp()

({ expr; })
parsing

for(...){
}else{
	printf("for not entered");
}

signed vs unsigned:
jl, jle, jg, jge
vs
jb, jbe, ja, jae - above + below
