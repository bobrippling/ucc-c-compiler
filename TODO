Ordered By Priority
-------------------

duplicate symbol check: "int x; x(){}"

fix:
{
	int i;
	do{
		int j; // no stack space reserved, this is specific to it being inside an if/do/while
	}while(0);
}

sizeof:
		sizeof(type)
	and
		sizeof `expr`

void f();
f(2); // valid
void g(void);
g(2); // invalid

fix 3 - 2 - 1 parsing

typedefs

struct parsing

32-bit code gen

general lhs assignment:
	(b ? x : y) = 5;
	^-- note: gnu extension
	(x ? a : b)(2); // similarly
	(x = y) = 2; // assign to x

assembly optimisation - remove asm_temp()

main(argc,argv)
char **argv;
{
}
parsing

({ expr; })
parsing

for(...){
}else{
	printf("for not entered");
}
